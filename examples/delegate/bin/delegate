#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require "delegate"

# Execute the Delegate command
begin
  Delegate.run(ARGV)
rescue Interrupt => e
  $stderr.puts "Interrupted"
rescue Minicom::Exit => e
  exit(e.exit_code)
end 
