<% if header %>
begin
  require 'quickl'
rescue LoadError
  require 'rubygems'
  gem 'quickl'
  retry  
end
<% end %>
#
# FIX: overview
#
# SYNOPSIS
#   #{program_name} [--version] [--help] [--output=...] COMMAND [cmd opts] ARGS...
#
# OPTIONS
# #{summarized_options}
#
# COMMANDS
# #{summarized_subcommands}
#
# DESCRIPTION
#   FIX: description 
#
# See '#{program_name} help COMMAND' for more information on a specific command.
#
class <%= cmd_class_name %> <  Quickl::Delegator(__FILE__, __LINE__)

  VERSION   = "1.0.0"

  # Install options
  options do |opt|
<%= option_helpers.collect{|s| tabto(s,4)}.join("\n") %>
  end
  
end # class <%= cmd_class_name %>
<% if footer %>
if __FILE__ == $0
  <%= cmd_class_name %>.run(ARGV)
end
<% end %>

